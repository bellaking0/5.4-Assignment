const names = ["Gary", "Spongebob", "Patrick", "Squidward"];
let x = 0;
let text = "x";
for (let x = 0; x < 4; x++) {
    Text += "Your Spongebob Squarepants character is" + x + "<p>"
}
while (names[x]) {
    text += names[x];
    x++;
}